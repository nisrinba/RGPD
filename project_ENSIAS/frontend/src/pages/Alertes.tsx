
import React from 'react';
import { Layout } from '../components/Layout';
import AlertesManagement from '../components/alertes/AlertesManagement';

const Alertes = () => {
  return (
    <Layout>
      <AlertesManagement />
    </Layout>
  );
};

export default Alertes;
