
import React from 'react';
import { Layout } from '../components/Layout';
import ConfigurationSettings from '../components/configuration/ConfigurationSetting';

const Configuration = () => {
  return (
    <Layout>
      <ConfigurationSettings />
    </Layout>
  );
};

export default Configuration;
